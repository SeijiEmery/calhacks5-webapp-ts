import * as React from 'react';
import '../App.css';

interface Props {
  onSubmit (): void;
}

const CameraPage = (props: Props) => {
  return (
    <div>
      <p>Camera...</p>
    </div>
  );
};
export default CameraPage;
