import * as React from 'react';
import '../App.css';

interface IProps {
  onSubmit (): void;
}

const CameraPage = (props: IProps) => {
  return (
    <div>
      <p>Camera...</p>
    </div>
  );
};
export default CameraPage;
